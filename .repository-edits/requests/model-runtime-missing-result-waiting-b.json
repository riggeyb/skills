{"version":2,"operation":"replace-unique","path":"sentient-orchestrator/tests/model-worker-runtime.test.ts","expected_source_git_blob_sha":"e13bab30b77e8545648188ff516d7b3678ac5112","old":"      const candidate = await db.query(\n        `SELECT wa.external_id,wa.status,wa.handoff,sw.id AS worker_id,sw.runtime_id,sw.status AS worker_status\n         FROM worker_assignments wa\n         JOIN sentient_workers sw ON sw.id=wa.target_worker_id\n         WHERE wa.task_id=$1\n           AND sw.role<>'lead'\n           AND sw.status='completed'\n           AND wa.status<>'accepted'\n         ORDER BY wa.created_at,wa.id\n         LIMIT 1`,\n        [task.id],\n      );\n      if (candidate.rowCount === 1) {\n        const row = candidate.rows[0];\n        assert.equal(row.runtime_id, \"model-backed\");\n        assert.equal(row.worker_status, \"completed\");","new":"      const candidate = await db.query(\n        `SELECT wa.external_id,wa.status,wa.handoff,sw.id AS worker_id,sw.runtime_id,sw.status AS worker_status\n         FROM worker_assignments wa\n         JOIN sentient_workers sw ON sw.id=wa.target_worker_id\n         WHERE wa.task_id=$1\n           AND sw.role<>'lead'\n           AND sw.status='waiting'\n           AND wa.status<>'accepted'\n         ORDER BY wa.created_at,wa.id\n         LIMIT 1`,\n        [task.id],\n      );\n      if (candidate.rowCount === 1) {\n        const row = candidate.rows[0];\n        assert.equal(row.runtime_id, \"model-backed\");\n        assert.equal(row.worker_status, \"waiting\");"}